/*
import 'package:flutter_share/flutter_share.dart';

class SharingUtils {
  Future<void> shareLink(String url, {String title, String text}) async {
    await FlutterShare.share(
      title: title != null ? title : 'Sharing link',
      text: text != null ? text : 'Sharing link',
      linkUrl: url,
    );
  }

  Future<void> shareLocalFile(String filePath,
      {String title, String text}) async {
    await FlutterShare.shareFile(
      title: title != null ? title : 'Sharing file',
      text: text != null ? text : 'Sharing file',
      filePath: filePath,
    );
  }
}
*/
