import 'notice_detail_model.dart';
import 'notice_list_model.dart';

// import 'package:sso_futurescape/presentor/module/chsone/notices/model_response.dart';
abstract class NoticeView extends ListNoticeModelResponse {
  // onError(var error);

  // onFailure(var failed);

  // onSuccess(String response);
}

abstract class NoticeDetailsView extends NoticeDetailModelResponse {
  // onError(var error);

  // onFailure(var failed);

  // onSuccess(String response);
}
